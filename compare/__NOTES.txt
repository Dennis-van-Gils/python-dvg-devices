**** Bronkhorst

    Ready for SerialDevice using method '`query'

**** Compax

    Ready for SerialDevice using method '`query'
    BUT
    Needs extra check at finish of query::

    ans_str = ans_bytes.decode("utf8").strip()
    if ans_str[0] == ">":
        # Successful operation without meaningful reply
        success = True
    elif ans_str[0] == "!":
        # Error reply
        print("COMPAX3 COMMUNICATION ERROR: " + ans_str)
    else:
        # Successful operation with meaningful reply
        success = True
        
        
**** ThermoFlex

    Works on binary, not ASCII
    
    Use super() in query to add:
    if ans_bytes is None:
        ans_bytes = np.nan